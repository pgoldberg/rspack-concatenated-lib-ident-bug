{
  "name": "app",
  "version": "0.0.0",
  "exports": {},
  "scripts": {
    "build": "rm -rf build && \"$npm_execpath\" --filter lib-dll run build && \"$npm_execpath\" exec rspack"
  },
  "dependencies": {
    "lib": "workspace:*"
  },
  "devDependencies": {
    "@rspack/cli": "^1.3.15",
    "@rspack/core": "^1.3.15"
  }
}
